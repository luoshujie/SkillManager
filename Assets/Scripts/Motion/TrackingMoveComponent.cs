using UnityEngine;

namespace Motion
{
    /// <summary>
    /// 子弹运动组件
    /// </summary>
    public class TrackingMoveComponent : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
